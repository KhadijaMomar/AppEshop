{% extends 'base.html.twig' %}

{% block title %}Nouveau produit{% endblock %}

{% block body %}
<style>
    .form-control {
        border-radius: 10px;
        padding: 10px 12px;
        border: 1px solid #ddd;
        transition: all 0.3s ease;
        box-shadow: none;
    }

    .form-control:focus {
        border-color: #4a90e2;
        box-shadow: 0 0 0 3px rgba(74, 144, 226, 0.2);
    }

    label {
        font-weight: 500;
        margin-bottom: 5px;
    }

    .card {
        border: none;
        border-radius: 20px;
    }

    .btn-primary {
        border-radius: 10px;
        padding: 10px;
        font-weight: 600;
        transition: 0.3s;
    }

    .btn-primary:hover {
        background-color: #357abd;
    }
</style>

<div class="container d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card shadow-lg p-4" style="width: 100%; max-width: 500px;">
        <div class="text-center mb-4">
            <h2 class="fw-bold">Ajouter un produit</h2>
            <p class="text-muted">Créez rapidement un nouveau produit</p>
        </div>

        {{ form_start(form) }}

        <div class="mb-3">
            {{ form_label(form.name) }}
            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.name) }}
        </div>

        <div class="mb-3">
            {{ form_label(form.description) }}
            {{ form_widget(form.description, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.description) }}
        </div>

        <div class="mb-3">
            {{ form_label(form.priceHT) }}
            {{ form_widget(form.priceHT, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.priceHT) }}
        </div>

        <div class="mb-3">
            {{ form_label(form.category) }}
            {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.category) }}
        </div>
        <div class="form-check mb-3">
            {{ form_widget(form.available, {'attr': {'class': 'form-check-input'}}) }}
            {{ form_label(form.available, null, {'label_attr': {'class': 'form-check-label'}}) }}
            {{ form_errors(form.available) }}
        </div>

        <button type="submit" class="btn btn-primary w-100 mt-3">
            Créer le produit
        </button>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}